module sum_leds(input clk, reset, [3:0] a, b,
    output  [5:0] leds
);
    reg [5:0] sum;
    begin
    always @(posedge clk or posedge reset) 
        if (reset)
            sum <= 6'b0;
        else
            sum <= a + b;
    end
    
    assign leds = sum;
    
endmodule

