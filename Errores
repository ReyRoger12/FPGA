module dip_switch_led (
    input [7:0] dip_switch,   // Entrada de los 8 dip switches
    output [7:0] led          // Salida de los 8 LEDs
);
    
    assign led = dip_switch;   // Los LEDs reflejan directamente el valor de los dip switches
    
endmodule
